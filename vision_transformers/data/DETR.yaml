# Images and labels direcotry should be relative to train.py
TRAIN_DIR_IMAGES: '../../object_detection/input/new merge.v4i.voc/train'
TRAIN_DIR_LABELS: '../../object_detection/input/new merge.v4i.voc/train'
VALID_DIR_IMAGES: '../../object_detection/input/new merge.v4i.voc/valid'
VALID_DIR_LABELS: '../../object_detection/input/new merge.v4i.voc/valid'

# Class names.
CLASSES: ['_background_'api', 'asap']

# Number of classes (object classes + 1 for background).
NC: 3

# Whether to save the predictions of the validation set while training.
SAVE_VALID_PREDICTION_IMAGES: True